VITE_API_URL=https://your-backend.onrender.com

# Public half of the VAPID keypair used for daily reminder push notifications
# (see backend/.env.example) — generate with: npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=xxxx
